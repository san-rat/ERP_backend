name: CD Dev - Migrate dev - Build & Push AuthService to ACR

on:
  push:
    branches: [ "dev" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install mysql client
        run: sudo apt-get update && sudo apt-get install -y mysql-client

      - name: Apply DB migrations (Azure MySQL)
        shell: bash 
        env: 
          MYSQL_HOST: ${{ secrets.AZ_MYSQL_HOST }}
          MYSQL_DB: ${{ secrets.AZ_MYSQL_DB }}
          MYSQL_USER: ${{ secrets.AZ_MYSQL_MIGRATOR_USER }}
          MYSQL_PASSWORD: ${{ secrets.AZ_MYSQL_MIGRATOR_PASSWORD }}
        run: |
          bash ./scripts/apply_migrations.sh db/auth/migrations

      - name: Docker login to ACR
        run: echo "${{ secrets.ACR_PASSWORD }}" | docker login ${{ secrets.ACR_LOGIN_SERVER }} -u ${{ secrets.ACR_USERNAME }} --password-stdin

      - name: Build and push image
        run: |
          IMAGE_SHA="${{ secrets.ACR_LOGIN_SERVER }}/authservice:${{ github.sha }}"
          IMAGE_LATEST="${{ secrets.ACR_LOGIN_SERVER }}/authservice:latest"

          docker build -t "$IMAGE_SHA" -t "$IMAGE_LATEST" -f src/AuthService/Dockerfile src/AuthService
          docker push "$IMAGE_SHA"
          docker push "$IMAGE_LATEST"

      - name: Smoke test (health + db-test)
        run: | 
          echo "Waiting 20s for Container App revision..."
          sleep 20
          curl -fsS "${{ secrets.AUTH_SERVICE_URL }}/health"
          curl -fsS "${{ secrets.AUTH_SERVICE_URL }}/db-check"